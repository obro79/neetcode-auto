[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "python -m app.jobs.send_daily"
cronSchedule = "*/15 * * * *"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 1
