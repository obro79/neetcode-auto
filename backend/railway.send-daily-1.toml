[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "python -m app.jobs.send_daily --attempt 1"
cronSchedule = "0 14 * * *"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 1
